{
  "records": [
    {
      "sensor_type": "(gas/bio/liquid; please choose one)",
      "detect_target": "(target chemical name)",
      "lower_detection_limit": "Provide the value as a string in ppm. **Always include the unit 'ppm' (e.g., '10 ppm')**. Copy the value exactly as it appears in the source text, including units (e.g., '10 ppm'). **Extract the lowest concentration reported in the experiments (i.e., the smallest value tested) as the lower_detection_limit.** When filling `lower_detection_limit`, use the reported limit of detection (LOD) from the paper (e.g., 1.18 µM for the CCN sensor, converted to ppm if necessary). If the paper gives a range, use the lower bound of the widest overall dynamic range. If the paper reports the analyte concentration as a fraction of its equilibrium vapor pressure (P/Pv), first convert it to ppm using Pv = 1.8 kPa and the total pressure ≈101.3 kPa (295 K). Otherwise, follow the original rules: if the paper explicitly reports a detection limit (e.g., “detection limit = …”, “LOD = …”, “minimum detectable concentration …”), use that value; if a concentration range is provided (even when not labeled as a detection limit, e.g., ‘detectable from 5 ppm to 1000 ppm’ or ‘1 ppm – 10 mM’), use the lower bound of the widest overall dynamic range (treat the lower bound as the lower_detection_limit); if only a single concentration value is reported, use that value as the lower limit; otherwise set \"\". If both a minimum detection limit and a range are reported, set lower_detection_limit to the minimum value.",
      "upper_detection_limit": "Provide the value as a string in ppm. **Always include the unit 'ppm' (e.g., '100 ppm')**. Copy the value exactly as it appears in the source text, including units (e.g., '100 ppm'). **Extract the highest concentration reported in the experiments (i.e., the largest value tested) as the upper_detection_limit.** When filling `upper_detection_limit`, use the highest analyte concentration that was measured in the study (e.g., 250 µM for the CCN sensor, converted to ppm if necessary). If the paper reports the analyte concentration as a fraction of its equilibrium vapor pressure (P/Pv), first convert it to ppm using Pv = 1.8 kPa and the total pressure ≈101.3 kPa (295 K). Otherwise, follow the original rules: if the paper mentions the highest concentration tested or the upper bound of a reported range (e.g., “up to 100 ppm”, “dynamic range: 1 µM–10 mM”, ‘detectable from 5 ppm to 1000 ppm’, or a sensing range like ‘1 ppm – 10 ppm’), use that value; if a concentration range is given, use the upper bound of the widest overall dynamic range (treat the upper bound as the upper_detection_limit); if only a single concentration is reported, set this field to \"\"; otherwise set \"\". When both a minimum detection limit and a range are reported, set upper_detection_limit to the maximum value of the range.",
      "probe_material": "(primary functional material(s) directly interacting with the analyte, e.g., 'Au')",
      "test_operating_temperature (celcius)": "xx °C; if not reported, assume a standard laboratory temperature of 25 °C",
      "pH_value": "(use -1 for gas, otherwise a number, if pH sensor, use range like xx-yy)",
      "test_medium": "(the carrier gas used in the experiment, e.g., nitrogen)"
    }
    (Select the sensor that achieves the lowest limit of detection (LOD) among the devices described and return a single JSON object for that sensor.)
  ]
}